import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
	appId: 'com.iathings.gym',
	appName: 'gym',
	webDir: 'build',
	bundledWebRuntime: false
};

export default config;
